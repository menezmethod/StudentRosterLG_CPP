#pragma once
#include <string>
#include <vector>

enum class DegreeProgram { UNDECIDED, SECURITY, NETWORK, SOFTWARE };

const std::vector<std::string> degreeProgramStr = {
    "UNDECIDED", "SECURITY", "NETWORK", "SOFTWARE"
};